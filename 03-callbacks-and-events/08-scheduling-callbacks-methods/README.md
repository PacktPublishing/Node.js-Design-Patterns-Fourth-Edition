# 08-scheduling-callbacks-methods

This example shows how the event loop priorities callbacks scheduled with
`setImmediate(cb)`, `setTimeout(cb, 0)` and `process.nextTick(cb)`.

## Run

To run the example launch:

```bash
node index.js
```
